#include<stdio.h>
#include<conio.h>

int main(){

int age;
printf("enter an age");
scanf("%d",&age);
(age>=18)? printf("you are aligible for open to account"): printf("you are not aligible for open to account");

return 0;
}